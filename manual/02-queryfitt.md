# Queryfitt

- `queryfitt::query` models pattern, traversal, path, reachability, and aggregation clauses.
- `queryfitt::parse_text()` is a parser boundary placeholder; Matcheroni-based structure parsing is intended for later implementation.
