# Chapter 07 — Traversal Basics

## Core Helpers (`grafitt::algo`)
- `reachable(g, src, dst)`
- `shortest_path(g, src, dst)` (unweighted BFS path)
- `bfs_order(g, root)`
- `degree(g, v)`
- `vertices(g)`, `edges(g)`

These primitives are building blocks for richer workflows.
